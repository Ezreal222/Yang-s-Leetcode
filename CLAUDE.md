Languages: C++ (primary), Python, JavaScript. 

Comments — C++: only at non-obvious algorithmic steps.
Comments — Python/JS: minimal when it's a direct translation of C++; heavy and educational when the solution uses idiomatic features or libraries (collections.Counter, heapq, bisect, itertools, Map/Set, spread, reduce, comprehensions, etc.). Explain what it does, why it's used here, rough C++ equivalent. The Python/JS tab should teach the language, not just translate.